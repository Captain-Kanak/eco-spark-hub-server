enum UserRole {
    ADMIN
    MEMBER
}

enum UserStatus {
    ACTIVE
    DEACTIVE
}

enum IdeaStatus {
    PENDING
    APPROVED
    REJECTED
}

enum VoteType {
    UP
    DOWN
}

enum PaymentStatus {
    PAID
    UNPAID
}
